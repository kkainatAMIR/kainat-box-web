# HTTPS endpoint that accepts JSON form submissions and sends notifications.
# Works with Formspree, Make, Zapier, a serverless function, or your own API.
VITE_FORMS_ENDPOINT=

# Add provider keys only through a secure backend/serverless function.
# Never expose Stripe/PayFast/PayPro secret keys in VITE_ variables.
